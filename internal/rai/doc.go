// Package rai implements the RelayAPI agent launcher.
//
// The CLI signs into a RelayAPI deployment, stores a device credential, and
// starts the real coding-agent executable with a per-launch provider contract.
package rai
