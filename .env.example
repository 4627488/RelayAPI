RELAY_DATA_PLANE=native
DATABASE_URL=postgres://relay:change-me@postgres:5432/relay?sslmode=disable
# Only required when RELAY_DATA_PLANE=cpa (legacy sidecar mode).
CPA_URL=http://cliproxyapi:8317
CPA_API_KEY=
CPA_MANAGEMENT_KEY=
# CPA data-plane safety limits. Defaults are conservative for a 512 MiB CPA container.
CPA_MAX_IN_FLIGHT=16
CPA_MAX_QUEUE=32
CPA_QUEUE_TIMEOUT_MILLISECONDS=2000
CPA_MAX_REQUEST_MIB=16
CPA_REQUEST_BYTES_IN_FLIGHT_MIB=32
CPA_REQUEST_TIMEOUT_SECONDS=600
CPA_CIRCUIT_FAILURE_THRESHOLD=3
CPA_CIRCUIT_OPEN_SECONDS=15
# Compose-level CPA memory envelope (soft Go limit below the hard cgroup limit).
CPA_GOMEMLIMIT=384MiB
CPA_MEMORY_LIMIT=512m
CPA_MEMORY_RESERVATION=384m
# Relay now embeds CPA's compatibility executors. Bound its Go heap and clear
# replay caches before the container reaches the hard limit.
RELAY_GOMEMLIMIT=576MiB
RELAY_MEMORY_LIMIT=768m
RELAY_MEMORY_RESERVATION=512m
RELAY_EXECUTOR_CACHE_PRESSURE_MIB=384
# Required when relayapi-bridge or parent/child subscriptions are enabled.
CPA_PLUGIN_SECRET=change-this-plugin-shared-secret
# Poll normalized credential quota through the CPA bridge (minimum 60 seconds).
CPA_QUOTA_SYNC_INTERVAL_SECONDS=300
RELAY_SESSION_SECRET=change-this-session-secret-at-least-32-characters
# Stable key used to encrypt recoverable tenant API keys. Do not rotate it
# without re-encrypting stored keys. Defaults to RELAY_SESSION_SECRET when empty.
RELAY_API_KEY_ENCRYPTION_KEY=change-this-api-key-encryption-key-at-least-32-characters
RELAY_PUBLIC_URL=http://localhost:8080
RELAY_SECURE_COOKIES=false
UNPRICED_MODEL_POLICY=allow
REQUEST_LOG_RETENTION_DAYS=30
# Error and incomplete request details. Successful details use the shorter window below.
REQUEST_LOG_DETAIL_RETENTION_DAYS=14
REQUEST_LOG_SUCCESS_DETAIL_DAYS=1
# Deterministic 1% sample of successful, fully-priced requests (parts per million).
REQUEST_LOG_SUCCESS_SAMPLE_PPM=10000
CPA_LIFECYCLE_SUCCESS_HOURS=24
CPA_LIFECYCLE_ERROR_DAYS=7
REQUEST_RESERVATION_RETENTION_DAYS=14
INCOMPLETE_RESERVATION_RETENTION_DAYS=90
QUOTA_OBSERVATION_RETENTION_DAYS=180
INVITATION_RETENTION_DAYS=30
RETENTION_BATCH_SIZE=5000
RETENTION_MAX_RUNTIME_SECONDS=30
BILLING_RESERVE_NANO_USD=10000000
