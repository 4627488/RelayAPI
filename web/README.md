# RelayAPI 控制台

React + TypeScript + Vite + Tailwind CSS v4 + **Cloudflare Kumo**。视觉合同见仓库根目录 `DESIGN.md`。页面按 `/api` 与 `/api/admin` 资源组织。

## 开发

```bash
pnpm install
pnpm dev
```

请求会代理到本机 `http://localhost:3000` 的 Go 服务。

```bash
pnpm check
```
